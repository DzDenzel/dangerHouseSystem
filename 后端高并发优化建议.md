# 危房智诊系统后端高并发优化建议

本文档基于当前项目后端实现进行静态架构检查，聚焦高并发场景下的主要瓶颈与优化方向。内容分为四部分：AI 检测与后端交互、Redis 缓存、MySQL 数据库、其他业务。

## 1. AI 检测与后端交互部分

### 1.1 当前问题

当前 AI 检测调用属于同步阻塞模型。后端在 HTTP 请求线程中直接调用 Python AI 服务，典型位置包括：

- `DetectionServiceImpl.startDetection`
- `DetectionServiceImpl.uploadAndDetectMultiple`
- `AiDetectionClient.detectDamage`

该流程中包含文件读取、Multipart 请求组装、AI HTTP 调用、AI 推理等待、结果图片 Base64 返回、结果图片保存、数据库状态更新等操作。AI 调用默认读取超时时间为 30 秒，一旦并发请求增多，会长时间占用 Tomcat 请求线程和数据库连接资源。

Python AI 服务侧也存在瓶颈。`api_server.py` 中单次请求虽然支持多图片上传，但内部按图片逐张串行推理，并且将结果图片编码为 Base64 返回。高并发时 CPU/GPU、内存、网络响应体大小都会成为瓶颈。

### 1.2 高并发风险

- 请求线程被 AI 推理长时间占用，普通接口也会受影响。
- 用户重复点击检测可能导致同一任务重复推理。
- AI 服务无并发隔离，容易被突发请求打满。
- Base64 结果图放大响应体，增加内存、网络和序列化成本。
- 后端事务包裹 AI 调用和文件 IO，容易造成数据库连接长时间占用。
- AI 服务异常或变慢时，后端缺少熔断、限流、降级机制。

### 1.3 优化建议

优先将 AI 检测改为异步任务模型：

1. 用户提交检测后，后端只创建检测任务并返回 `taskId`。
2. 后端将检测任务写入队列或任务表，状态为 `PENDING`。
3. 后台 worker 拉取任务，更新为 `PROCESSING`，再调用 AI 服务。
4. AI 完成后更新为 `COMPLETED`，失败则更新为 `FAILED`，记录错误原因。
5. 前端通过轮询、SSE 或 WebSocket 获取任务状态。

建议增加任务并发控制：

- 按系统维度限制同时运行的 AI 任务数。
- 按用户维度限制同时运行的 AI 任务数。
- 对同一 `detectionId` 加分布式锁，避免重复启动。
- 任务超时后自动标记失败或进入重试队列。

建议优化 AI 服务响应：

- AI 服务只返回结构化检测结果和结果图片地址，不返回大体积 Base64。
- 结果图片由 AI 服务或后端直接上传到 OSS/本地文件系统，再返回 URL。
- 限制单次请求图片数量、图片大小和最大分辨率。
- 根据 GPU 显存和模型耗时设置 worker 数，不盲目增加进程。

建议增加服务保护：

- 后端调用 AI 服务使用连接池 HTTP 客户端。
- 增加连接超时、读取超时、连接租借超时。
- 引入熔断、限流、Bulkhead 舱壁隔离。
- AI 服务不可用时快速失败，避免拖垮后端主服务。

### 1.4 优先级

高优先级：

- AI 检测异步化。
- 移除请求线程内长时间 AI 推理等待。
- AI 任务增加并发上限和幂等控制。

中优先级：

- 结果图片改为 URL 返回。
- AI 服务水平扩容和 GPU 资源隔离。
- 增加熔断、限流、重试和任务超时机制。

## 2. Redis 缓存部分

### 2.1 当前问题

项目中已经引入 Redis，并实现了缓存穿透保护、互斥锁、TTL 抖动等能力，这是一个好的基础。但当前缓存实现仍有几个高并发风险：

- 列表缓存删除使用 `deleteByPrefix`，内部通过 scan 查找 key。
- 检测、建筑等列表缓存更新时会频繁按前缀扫描删除。
- Redis 连接池 `max-active` 配置较小，当前为 8。
- Redis 连接等待时间为 `-1ms`，表示无限等待。
- MyBatis `Page` 对象直接进入缓存，泛型反序列化不稳定。

### 2.2 高并发风险

- 高频写入时，前缀扫描删除会放大 Redis 压力。
- 缓存 key 数量增加后，scan 虽然比 keys 安全，但仍会带来额外开销。
- Redis 连接池过小，容易导致请求等待 Redis 连接。
- 无限等待会导致业务线程堆积，进一步拖慢整个服务。
- 分页对象缓存反序列化失败会导致缓存频繁失效，降低命中率。

### 2.3 优化建议

建议将列表缓存失效从“扫描删除”改为“版本号失效”：

```text
detection:list:v1:page:size:filter...
building:list:v3:page:size:filter...
```

当检测记录变化时，不扫描删除所有 `detection:list:*`，只递增 `detection:list:version`。新请求自动使用新版本 key，旧 key 依赖 TTL 自然过期。

建议调整 Redis 连接池：

- 根据压测结果调大 `max-active`。
- 设置有限 `max-wait`，例如 100ms 到 500ms。
- 保持较合理的 `min-idle`，避免突发流量时频繁建连。
- Redis 异常时允许业务降级回源数据库，但必须有限流保护。

建议优化缓存对象：

- 不直接缓存 MyBatis `Page`。
- 改为缓存自定义 `PageResponse<T>`。
- 缓存 DTO 应保持字段稳定，避免框架对象序列化问题。

建议区分缓存策略：

- 详情缓存适合较长 TTL，并使用互斥锁防击穿。
- 列表缓存 TTL 不宜过长，建议结合版本号失效。
- 仪表盘统计可以缓存，但更新频率高时建议异步刷新或定时预热。
- Token 黑名单属于安全缓存，应保持稳定访问和独立 key 空间。

### 2.4 优先级

高优先级：

- 去除高频业务中的前缀扫描删除。
- Redis 连接池改为有限等待。
- 分页缓存改为自定义 DTO。

中优先级：

- 建立列表缓存版本号机制。
- 仪表盘缓存改为定时预热。
- 增加 Redis 指标监控：命中率、慢命令、连接池等待、超时次数。

## 3. MySQL 数据库部分

### 3.1 当前问题

当前数据库已经为主要字段建立了部分单列索引，例如 `building_id`、`user_id`、`status`、`risk_level`、`detect_time`、`deleted` 等。但高并发和大数据量场景下，仅靠单列索引不足以支撑复杂筛选、分页排序和关联查询。

检测列表存在 N+1 查询问题。分页查出 detection 后，又对每条记录分别查询 building、user、image、report。数据量和页大小上来后，单个列表接口会产生大量 SQL。

建筑风险等级筛选存在全量查询再内存聚合的问题。当前逻辑会查询 detection 表中的风险等级数据，再在 Java 内存中按 building 聚合判断。这在数据量增长后会成为明显瓶颈。

部分业务方法在事务中执行文件 IO、AI HTTP 调用、结果保存等慢操作，事务时间过长，容易占用数据库连接。

### 3.2 高并发风险

- 列表接口 SQL 数量随分页条数线性增长。
- 单列索引无法有效覆盖多条件筛选和排序。
- `LIKE '%关键词%'` 查询在数据量大时难以利用普通索引。
- 长事务占用连接池，影响其他短查询。
- 仪表盘统计多次 `count`，高频访问会增加数据库压力。
- 报告、图片、检测结果等关联数据查询缺少批量加载策略。

### 3.3 优化建议

建议优先解决 N+1 查询：

- 分页先查 detection 主表。
- 收集 `buildingId`、`userId`、`detectionId`。
- 分别批量查询 building、user、image、report。
- 使用 Map 在内存组装响应。

或者为高频列表编写专用 Mapper SQL，通过 join 或子查询直接返回列表 DTO。

建议补充组合索引，方向如下：

```sql
-- 用户检测列表
CREATE INDEX idx_detection_user_deleted_time
ON detection(user_id, deleted, detect_time);

-- 建筑检测列表
CREATE INDEX idx_detection_building_deleted_time
ON detection(building_id, deleted, detect_time);

-- 状态筛选
CREATE INDEX idx_detection_status_deleted_time
ON detection(status, deleted, detect_time);

-- 风险等级筛选
CREATE INDEX idx_detection_risk_deleted_time
ON detection(risk_level, deleted, detect_time);

-- 图片批量查询
CREATE INDEX idx_image_detection_deleted
ON image(detection_id, deleted);

-- 报告按检测查询
CREATE INDEX idx_report_detection_deleted
ON report(detection_id, deleted);
```

实际索引顺序应结合真实 SQL、数据分布和 `EXPLAIN` 结果确认，避免盲目增加过多索引。

建议优化建筑风险筛选：

```sql
SELECT DISTINCT building_id
FROM detection
WHERE deleted = 0
  AND risk_level IN ('C', 'D');
```

如果普通用户只能看自己的数据，则增加 `user_id = ?` 条件，并配套组合索引。

建议缩短事务：

- 数据状态变更单独事务。
- 文件上传、AI 调用、PDF 生成不要放在长事务中。
- AI 成功后再用短事务写入检测结果。
- 失败时用短事务更新失败状态和错误信息。

建议优化统计类接口：

- 仪表盘数据缓存或定时预聚合。
- 高风险建筑数量可建立冗余字段或统计表。
- 对低实时性统计，避免每次请求直接 count 大表。

### 3.4 优先级

高优先级：

- 检测列表 N+1 查询优化。
- 建筑风险筛选改 SQL 聚合。
- 长事务拆分。
- 为核心列表查询补组合索引。

中优先级：

- 统计数据预聚合。
- 大表归档策略。
- 慢 SQL 日志和 `EXPLAIN` 巡检机制。

## 4. 其他业务部分

### 4.1 当前问题

除 AI、缓存、数据库外，项目在文件上传、报告生成、认证、日志、限流和配置方面也存在高并发优化空间。

当前文件上传最大请求为 50MB，检测图片和报告文件会占用较多带宽、磁盘和内存。报告生成使用同步 PDF 生成方式，生成过程包含数据读取、PDF 渲染、文件保存。如果多个用户同时生成报告，会占用请求线程和 CPU。

JWT 认证过滤器每次请求都会解析 token，并检查 Redis 黑名单，再加载用户信息。虽然项目中已经有认证用户缓存，但仍需要关注热点用户和 Redis 故障时的降级策略。

日志方面，当前配置开启 MyBatis SQL stdout 输出，项目包日志级别为 debug。高并发下控制台 SQL 日志和 debug 日志会显著影响吞吐。

系统缺少明显的接口限流和资源配额策略，上传、AI 检测、报告生成这类重接口如果被并发调用，容易影响普通查询接口。

### 4.2 高并发风险

- 大文件上传占用带宽、磁盘 IO 和请求线程。
- PDF 同步生成导致响应慢、CPU 抖动。
- 日志量过大造成 IO 压力。
- 重接口没有限流，容易拖垮整体服务。
- 静态文件本地存储不利于多实例部署。
- 健康检查、监控指标不足，故障定位困难。

### 4.3 优化建议

文件上传建议：

- 对图片大小、格式、分辨率进行严格校验。
- 移动端上传前压缩图片。
- 生产环境优先使用 OSS，对象存储负责静态文件访问。
- 后端只保存文件元数据和访问 URL。
- 多实例部署时避免依赖本地磁盘共享。

报告生成建议：

- 报告生成改为异步任务。
- 同一检测记录的报告生成加幂等控制。
- 已生成报告直接复用，除非检测结果变化。
- PDF 文件生成后缓存下载 URL。

认证与安全建议：

- 用户信息缓存应保持稳定 TTL，并在用户信息变更时精确失效。
- Token 黑名单 key 使用 token hash，避免直接存储原始 token。
- Redis 不可用时要明确策略：安全优先则拒绝敏感请求，可用性优先则允许短暂降级。
- 生产环境关闭 Swagger 或加访问控制。

限流与隔离建议：

- 登录接口按 IP 和账号限流。
- 上传接口按用户限流。
- AI 检测接口按用户和全局并发数限流。
- 报告生成接口按检测记录和用户限流。
- 普通查询接口和重任务接口使用不同线程池或不同服务实例隔离。

日志与配置建议：

- 生产环境关闭 MyBatis SQL stdout。
- 项目日志级别调整为 info 或 warn。
- 操作日志异步写入已有基础，但线程池应配置拒绝策略和队列告警。
- 数据库、Redis、JWT 密钥、OSS 配置全部使用环境变量，不在配置文件中写死。

监控与压测建议：

- 增加接口 P95/P99 响应时间监控。
- 增加 JVM 线程数、GC、堆内存、连接池指标。
- 增加 MySQL 慢 SQL、连接池使用率监控。
- 增加 Redis 命中率、慢命令、连接池等待监控。
- 增加 AI 服务推理耗时、队列长度、GPU/CPU 使用率监控。
- 使用 JMeter、k6 或 Gatling 建立压测基线。

### 4.4 优先级

高优先级：

- 关闭生产 SQL stdout 和 debug 日志。
- 上传、AI 检测、报告生成增加限流。
- 报告生成异步化。
- 文件存储从本地磁盘迁移到 OSS 或统一文件服务。

中优先级：

- 增加监控指标和告警。
- 建立压测脚本和容量基线。
- 将配置和密钥全部环境变量化。

## 总体落地顺序建议

第一阶段，先处理最容易引发系统雪崩的问题：

1. AI 检测异步化。
2. AI 检测增加并发上限和幂等控制。
3. 关闭 SQL stdout 和 debug 日志。
4. Redis 连接池设置有限等待。

第二阶段，处理数据库和缓存性能问题：

1. 检测列表 N+1 查询优化。
2. 建筑风险筛选改为 SQL 聚合。
3. 核心查询补组合索引。
4. 列表缓存改为版本号失效。

第三阶段，完善工程化能力：

1. 报告生成异步化。
2. 文件上传接入 OSS。
3. 增加熔断、限流、监控和告警。
4. 建立压测基线和容量评估流程。
